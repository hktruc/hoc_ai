document.addEventListener('DOMContentLoaded',()=>{const boxes=[...document.querySelectorAll('[data-lesson]')];const key='hoc_ai_progress_v2';let saved={};try{saved=JSON.parse(localStorage.getItem(key)||'{}')}catch(e){}boxes.forEach(b=>{b.checked=!!saved[b.dataset.lesson];b.addEventListener('change',()=>{saved[b.dataset.lesson]=b.checked;localStorage.setItem(key,JSON.stringify(saved));update()})});function update(){const all=28;const done=Object.values(saved).filter(Boolean).length;document.querySelectorAll('[data-progress-label]').forEach(el=>el.textContent=`${done} / ${all} bài đã hoàn thành`);document.querySelectorAll('[data-progress-bar]').forEach(el=>el.style.width=`${Math.round(done/all*100)}%`)}update()});